{
  "name": "ui-monorepo",
  "private": true,
  "scripts": {
    "start": "lerna run start --parallel",
    "start:workspaces": "lerna run start --scope @tickle/workspaces --scope @tickle/ui-kit --parallel",
    "setup": "npx lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.10.2"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
